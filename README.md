# Javelin

Javelin is local, agent-native version control for immutable accepted World Versions and isolated tentative Private Layers. It is independent of Git at runtime.

Version `1.0.0` is under active development.

